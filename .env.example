# C3 - Claude Code Chat over Signal
# Copy to .env and fill in your values

# Your Signal account number (registered with signal-cli)
C3_ACCOUNT=+44...

# Comma-separated list of allowed sender numbers (empty = allow all)
C3_ALLOWED=+44...

# Claude model to use (opus, sonnet, haiku)
C3_MODEL=opus

# Max budget per message in USD
C3_MAX_BUDGET=5.0
